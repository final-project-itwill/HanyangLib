<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	
	<insert id="memberinsert">
		INSERT INTO member2(m_id, m_pw, m_name, m_nick,m_birth,m_gender
		,m_zip,m_add1,m_add2,m_tel, m_email, m_mailcheck,m_smscheck
		,m_gudok,m_rdate)
		
		VALUES (#{m_id}, #{m_pw}, #{m_name},#{m_nick}, #{m_birth}, #{m_gender}
		,#{m_zip},#{m_add1},#{m_add2},#{m_tel},#{m_email},#{m_mailcheck}
		,#{m_smscheck},#{m_gudok},now())
	</insert>
	
	<select id="detail" resultType="kr.co.itwill.member.MemberDTO">
		SELECT m_id, m_nick, m_pw, m_email, m_zip, m_add1, m_add2, m_mailcheck, m_smscheck
		FROM member
		WHERE m_id = #{s_id}
	</select>
	
	<update id="update">
		UPDATE member
		SET m_nick=#{m_nick}, m_pw=#{m_pw}, m_email=#{m_email}, m_zip=#{m_zip}
		, m_add1=#{m_add1}, m_add2=#{m_add2}, m_mailcheck=#{m_mailcheck}, m_smscheck=#{m_smscheck}
		WHERE m_id=#{s_id}
	</update>

	
	
	
	
</mapper>